# benchmark-core

Lightweight benchmark module used for structural Phase 3 performance smoke checks.

## Task

- `:jParser:benchmark:benchmark-core:perf_smoke`

## What it runs

`PerfSmokeMain` executes simple copy microbench loops and prints `ns/op` values for CI visibility.

