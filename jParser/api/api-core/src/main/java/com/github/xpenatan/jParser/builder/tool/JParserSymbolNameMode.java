package com.github.xpenatan.jParser.builder.tool;

/** Symbol naming policies supported by jParser binding generators. */
public enum JParserSymbolNameMode {
    /** Uses the default generated symbol names. */
    DEFAULT,

    /** Uses obfuscated generated symbol names. */
    OBFUSCATED
}
