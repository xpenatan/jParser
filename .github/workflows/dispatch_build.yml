name: Dispatch Build

on:
  workflow_dispatch:
    inputs:
      isRelease:
        required: true
        type: boolean
      shouldUpload:
        required: true
        type: boolean
jobs:
  build-and-upload:
#    uses: ./.github/workflows/build_and_upload.yml
#    with:
#      isRelease: ${{ inputs.isRelease }}
#      shouldUpload: ${{ inputs.shouldUpload }}
#    secrets: inherit
#
    runs-on: ubuntu-latest
    steps:

      - name: Test Upload
        uses: actions/github-script@v7
        if: ${{ inputs.shouldUpload }}
        with:
          script: |
            console.log("LOG shouldUpload");

      - name: Test Release
        uses: actions/github-script@v7
        if: ${{ inputs.isRelease }}
        with:
          script: |
            console.log("LOG RELEASE");